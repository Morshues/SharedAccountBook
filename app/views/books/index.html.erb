
<% @books.each do |book| %>
  <%= link_to book.name, book_path(book.token) %>
  <br>
<% end %>

<br>

<%= link_to t('book.new_book'), new_book_path %>

<br>

<%= link_to t('home.signout'), destroy_user_session_path, :method => :delete %>

